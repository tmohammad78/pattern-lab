## Overview

Repeatedly halve the search space on sorted or monotonic data. Instead of scanning every element, eliminate half the candidates at each step.

## When to Use

- Find target in sorted array
- Search in rotated sorted array
- Find peak element

*More episodes coming soon.*
