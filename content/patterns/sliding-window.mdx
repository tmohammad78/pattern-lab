## Overview

Maintain a window of elements that slides across an array or string. Instead of recomputing from scratch at each position, update the window incrementally by adding the entering element and removing the leaving one.

## When to Use

- Maximum/minimum sum subarray of size K
- Longest substring with K distinct characters
- Minimum window substring

*More episodes coming soon.*
